package org.example.legacy;

/**
 * Legacy / placeholder controller moved into a dedicated legacy package to avoid
 * conflicting with the real application controller. Left intentionally inert.
 */
public class AppControllerLegacy {

    // placeholder - no runtime behavior

}

